#Please specify either the version or the full url for the following components
node.version=11.0.1
ogmios.version=7.0.0
kupo.version=2.12.0

yaci.store.tag=rel-native-2.0.1-devkit
yaci.store.version=2.0.1-devkit
yaci.store.jar.version=2.0.1

yano.tag=v0.1.0-pre15
yano.version=0.1.0-pre15

#node.url=
# Ogmios/Kupo are published by CardanoSolutions for x86_64-linux, aarch64-linux
# and aarch64-macos. The Linux builds are statically linked; the macOS builds link
# only against system libraries. macOS x86_64 and Windows are not published : on
# those set a compatible custom URL here or use the DevKit Docker image.
#ogmios.url=
#kupo.url=
#yaci.store.url=
#yaci.store.jar.url=
#yano.url=

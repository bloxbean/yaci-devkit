{
  "name": "wallet-demo-tests",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "serve": "npx serve . -p 3456 -s"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0"
  }
}
